<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.virual.entornosvirtuales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menuprincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menuclases" />
        <activity android:name=".Mudulos" />
        <activity android:name=".Cusros" />
        <activity android:name=".ListaEstudiantes" />
        <activity android:name=".listaCursos" />
        <activity android:name=".perfilUsuario" />
        <activity android:name=".detalles" />
        <activity android:name=".listamodulos"></activity>
    </application>

</manifest>